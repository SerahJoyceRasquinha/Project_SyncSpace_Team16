# frontend/.env — CLIENT-SIDE config only.
#
# Anything prefixed VITE_ is compiled into the public JavaScript bundle and
# is readable by anyone using the site. NEVER put an API key here.
# Server secrets (including GEMINI_API_KEY) go in backend/.env instead.

VITE_SERVER_URL=http://localhost:5000
